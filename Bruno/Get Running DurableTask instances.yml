info:
  name: Get Running DurableTask instances
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/runtime/webhooks/durabletask/instances?code={{code}}&runtimeStatus=Running"
  params:
    - name: code
      value: "{{code}}"
      type: query
    - name: runtimeStatus
      value: Running
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
